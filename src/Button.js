import React from 'react'

const Button = () => {
    return (
        <div>
            <button className = 'button-add' type = "submit" > Add </button>
        </div>
    )
}

export default Button
